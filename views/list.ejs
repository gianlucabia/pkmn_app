<!DOCTYPE html>
<html>
  <head>
    <title>Pkmn_app</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Team list</h1>
    <hr>
    <table>
      <% var c=0 %>
      <tr>
      <% for(var i=0; i < data.length; i++) { %>
        
          <% if (i==0) {%> 
            <td> <a href=<%= data[i].id+"/edit" %>>Edit team </a></td>
            <td><%= data[i].id %></td>
            <td><%= data[i].name %></td>
          <%} else { %>     
            <% if (data[i].name!=data[i-1].name) {%>
              </tr><tr>
              <td> <a href=<%= data[i].id+"/edit" %>>Edit team </a></td>
              <td><%= data[i].id %></td>
              <td><%= data[i].name %></td>
            <% } %> 

          <% } %> 
          <td><%= pokeData.pokemons[i].id %></td>
          <% c++ %>

      <% } %>

      </tr>
      <% if(c==0) { %>
        <tr>
          <td>No teams created</td>
        </tr>
      <% } %>
    </table>

    <%= pokeData %>

    <p> <a href="/">Home</a></p>
  </body>
</html>
